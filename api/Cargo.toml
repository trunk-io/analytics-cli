[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
context = { path = "../context" }
serde = { version = "1.0.130", default-features = false, features = ["derive"] }
serde_json = "1.0.68"
